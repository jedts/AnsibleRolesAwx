# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: all
  become: yes

  tasks:

- name: Call Roles
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
